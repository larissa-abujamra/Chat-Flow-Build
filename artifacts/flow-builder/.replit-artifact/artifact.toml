kind = "web"
previewPath = "/"
title = "Flow Builder"
version = "1.0.0"
id = "artifacts/flow-builder"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25078

[services.development]
run = "pnpm --filter @workspace/flow-builder run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/flow-builder", "run", "build" ]
publicDir = "artifacts/flow-builder/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25078"
BASE_PATH = "/"
